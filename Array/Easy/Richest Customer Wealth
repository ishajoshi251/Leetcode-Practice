class Solution:
    def maximumWealth(self, accounts: List[List[int]]) -> int:
        maxwealth = 0
        for i in accounts:
            currentwealth = sum(i)
            maxwealth = max(currentwealth , maxwealth)
        return maxwealth
